Imports System.IO
Partial Class _Default
    Inherits System.Web.UI.Page

    'Protected Sub Btnshow_Click(ByVal sender As Object, ByVal e As System.EventArgs) Handles Btnshow.Click
    '    Dim path As String = TextBox1.Text
    '    Dim maindir As New DirectoryInfo(path)
    '    If maindir.Exists Then
    '        Dim subdir() As DirectoryInfo = maindir.GetDirectories()
    '        For i As Integer = 0 To subdir.Length - 1
    '            ListBox1.Items.Add("Name : " & subdir(i).ToString)
    '        Next i
    '    Else
    '        ListBox1.Items.Add("Main Directory Does not exists")
    '    End If

    'End Sub

    Protected Sub Btnshow_Click(ByVal sender As Object, ByVal e As System.EventArgs) Handles Btnshow.Click

    End Sub

    Protected Sub Button1_Click(ByVal sender As Object, ByVal e As System.EventArgs) Handles Button1.Click

    End Sub
End Class
