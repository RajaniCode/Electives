The folder contains various databases that needs to be attached to a sql server 2008 database. by default the project would look for
these databases on a default instance. If sql server 2008 is not installed on a default instance. change the connectionstring inside app.config of 
linq cookbook project.