5.4 Interface Naming

According to the convention for naming interfaces in Go, fill in the blank below with the proper name for an interface with a single method resolve() that has return type bool.

type [ ] interface {
    resolve() bool
}


Answer: resolver